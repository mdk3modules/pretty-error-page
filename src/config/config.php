<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Package Always Pretty
    |--------------------------------------------------------------------------
    |
    | By default the package will display pretty errors when `config.debug` is
    | disabled. You can override this with `true` for "display always" or
    | `false` for "never display".
    |
    */

    'always_pretty' => true,

];
